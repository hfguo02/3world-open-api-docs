// OpenAPI's request/response components call these extension points inside the schema tabs.
// Examples have a dedicated right rail in this site, so the middle column stays schema-only.
export const ResponseExamples = () => null;
export const ResponseExample = () => null;
export const ExampleFromSchema = () => null;
